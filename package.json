{
    "name": "tennda",
    "version": "1.0.0",
    "description": "Tennda application with separate frontend and backend",
    "scripts": {
      "start:frontend": "cd frontend && npm run dev",
      "start:backend": "cd backend && ./venv/bin/python app/app.py",
      "start": "concurrently 'npm run start:frontend' 'npm run start:backend'",
      "install:all": "npm install && cd frontend && npm install && cd ../backend && ./venv/bin/pip install -r requirements.txt"
    },
    "dependencies": {
      "concurrently": "^8.2.0"
    }
  }
  